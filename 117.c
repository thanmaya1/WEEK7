push('P');  // push 'P' into the stack
push('U');  // push 'U' into the stack
pop();      // pop the top element from the stack ('U' is removed)
push('S');  // push 'S' into the stack
push('H');  // push 'H' into the stack
pop();      // pop the top element from the stack ('H' is removed)
push('P');  // push 'P' into the stack
pop();      // pop the top element from the stack ('P' is removed)
push('A');  // push 'A' into the stack
